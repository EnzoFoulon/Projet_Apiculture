{# templates/apiculteur/account.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Compte de l'apiculteur{% endblock %}

{% block body %}
    <h1>Compte de l'apiculteur</h1>
    
    <h2>Informations de l'apiculteur</h2>
    <p>Nom d'utilisateur : {{ apiculteur.username }}</p>
    <p>Email : {{ apiculteur.email }}</p>

    {% if ruches is defined and ruches|length > 0 %}
        <h2>Liste de ruches</h2>
        <ul>
            {% for ruche in ruches %}
                <li>Ruche {{ ruche.id }} - Position : {{ ruche.position }}</li>
            {% endfor %}
        </ul>
    {% else %}
        <h2>Aucune ruche enregistrée</h2>
    {% endif %}
    
    {# Si vous avez une carte, incluez-la ici #}
    {# Sinon, affichez la liste des ruches avec leurs positions #}
    
    {# Exemple d'inclusion d'une carte OpenStreetMap (il faudra implémenter la logique JS) #}
    {# <div id="map" style="width: 100%; height: 400px;"></div> #}
    {# <script> #}
    {#     // JavaScript pour initialiser et marquer les positions des ruches sur la carte #}
    {# </script> #}
{% endblock %}
